import { redirect } from 'next/navigation'

export default function Home() {
  redirect('/waitlist')
}

// ORIGINAL HOME PAGE BACKED UP TO: page.tsx.backup
// The original home page with dashboard, animations, and auth has been preserved in page.tsx.backup
